ip addr show — your IPs and interfaces
ip route show — your routing table (critical for cloud routing later)
ping google.com — connectivity test
traceroute google.com (install: sudo apt install traceroute)
curl -I https://example.com — HTTP headers only
wget https://example.com -O /tmp/test.html — download
Commit: branch week1/day3, push, PR, merge
